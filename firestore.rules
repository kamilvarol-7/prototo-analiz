rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;
      allow write: if false; // Block all client-side writes. Writes are only allowed via the server-side Firebase Admin SDK.
    }
  }
}
